# Django Website Initialization


Create a Django project by
* initialize a Protry project
* add Django packages
* add code quality packages
* start a Django project
* start a Django app
* add the app to the INSTALLED_APPS list

## Usage
```
mk-django-all <package> [<python-version> <<license> <app-nmae> <project-name> <description>]
